import { readStore, writeStore, addActivity, money } from '../../../../../lib/store';
export const runtime='nodejs';
export async function POST(request,{params}){const {id}=await params,body=await request.json(),store=readStore(),rfq=store.rfqs.find(x=>x.id===id),quote=rfq?.quotes.find(x=>x.supplierId===Number(body.supplierId));if(!quote)return Response.json({error:'Offer not found.'},{status:404});const order={id:`ORD-${String(1001+store.orders.length).padStart(4,'0')}`,project:rfq.project,supplier:quote.supplier,amount:quote.quote,status:'Confirmed',eta:`${quote.delivery} hrs`,invoice:'GST invoice draft',credit:'Eligible for 30-day business credit',createdAt:new Date().toISOString()};rfq.status='Order confirmed';store.orders.unshift(order);addActivity(store,'order',`${order.id} confirmed with ${order.supplier}.`,order.id);writeStore(store);return Response.json({order,message:`Order confirmed for ${money(order.amount)}. GST draft and credit workflow created.`},{status:201})}
