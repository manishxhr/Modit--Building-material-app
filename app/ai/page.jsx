'use client'; import {useState} from 'react'; import Nav from '../components/Nav';
const inr=n=>'₹'+Number(n).toLocaleString('en-IN');
export default function AI(){const[run,setRun]=useState(),[loading,setLoading]=useState(false),[message,setMessage]=useState('');async function submit(e){e.preventDefault();setLoading(true);setMessage('');const f=Object.fromEntries(new FormData(e.currentTarget));const r=await fetch('/api/workflow/plan',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(f)});setRun(await r.json());setLoading(false)}async function accept(supplierId){const r=await fetch(`/api/rfqs/${run.rfq.id}/accept`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({supplierId})});const d=await r.json();setMessage(r.ok?`${d.message} Order ${d.order.id} is now visible in tracking.`:d.error)}return <><Nav/><main className="app"><section className="apphero"><div><p className="eyebrow">MODIT AI COPILOT / ACTIONABLE PROCUREMENT</p><h1>From requirement<br/>to <i>order-ready.</i></h1><p>MODIT reads your project context, builds a material plan and presents supplier trade-offs before a purchase order is created.</p></div><div className="aihint"><b>AI is decision support, not autopilot.</b><br/>Every supplier selection and order remains user-approved and auditable.</div></section><section className="workflow"><form onSubmit={submit}><p className="eyebrow">1 / SITE BRIEF</p><label>Project name<input name="project" required defaultValue="North Tower Residences"/></label><div className="twocol"><label>Project type<select name="projectType"><option value="residential">Residential</option><option value="commercial">Commercial</option><option value="fitout">Fit-out</option></select></label><label>Delivery zone<select name="city"><option>Gurugram</option><option>Noida</option><option>Delhi</option><option>Faridabad</option></select></label><label>Built-up area<input name="areaSqft" type="number" min="200" defaultValue="1800"/></label><label>Floors<input name="floors" type="number" min="1" defaultValue="2"/></label></div><label>BOQ / requirement notes<textarea name="requirements" defaultValue="Structure stage: cement, TMT steel, M-sand and AAC blocks. Delivery inside 48 hours."/></label><label>Working budget (₹)<input name="budget" type="number" defaultValue="500000"/></label><button className="button primary">{loading?'Analysing site…':'Build AI procurement plan →'}</button></form><div className="results">{!run&&!loading&&<div className="empty"><b>Ready to analyse.</b><p>Submit the site brief to generate a live BOM and comparison.</p></div>}{loading&&<div className="empty"><b>Structuring the requirement…</b><p>Checking material quantities and supplier fit.</p></div>}{run&&<><p className="eyebrow">2 / MATERIAL PLAN · {run.rfq.id}</p><h2>{run.summary}</h2><p className="muted">Estimated material value: <b>{run.total}</b></p><div className="bom">{run.rfq.materials.map(x=><div key={x.name}><span>{x.name}</span><b>{x.quantity}</b><small>{inr(x.estimate)}</small></div>)}</div><p className="eyebrow">3 / RANKED OFFERS</p><div className="offers">{run.rfq.quotes.map((x,i)=><article key={x.supplierId}><div><b>{x.supplier}</b><small>{x.reason} · {x.delivery}h delivery · ★ {x.rating}</small></div><strong>{inr(x.quote)}</strong><button className={i===0?'button primary':'button'} onClick={()=>accept(x.supplierId)}>Select</button></article>)}</div>{message&&<p className="notice">{message}</p>}</>}</div></section></main></>}
